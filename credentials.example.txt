# ─────────────────────────────────────────────────────────────
# CREDENTIALS — for creating accounts on external apply sites
#
# When a job listing redirects to a company careers page or an
# external ATS (Workday, Greenhouse, Lever, Ashby, iCIMS, …) that
# requires an account before you can apply, the agent uses the
# details below to register / sign in.
#
# Copy this file to `credentials.txt` and fill it in.
# `credentials.txt` is gitignored so your secrets stay on THIS
# machine only. It is never committed and never sent anywhere
# except into the login/registration form of the site you're
# applying to.
#
# Leave a line blank if you don't want the agent to use it. The
# agent will NEVER invent a value that's missing here — if a site
# needs something not listed, it stops and marks the row `Error`.
# ─────────────────────────────────────────────────────────────

## Email to register with
# Use a real inbox you can check — many ATS send a verification link.
email:

## Password to use for new accounts
# Pick a strong password. The agent reuses this for accounts it
# creates so you have one known credential across ATS sites.
password:

## Preferred sign-in method (optional)
# If you'd rather the agent use social sign-on when a site offers it,
# name it here: "google", "linkedin", "apple", or "email".
# The persistent browser profile (.browser-profile/) must already be
# logged into that provider — set that up via /setup-login.
# Leave blank to default to email + password registration.
sso_provider:

## Full name on the account (optional — falls back to info.txt)
full_name:

## Phone for accounts that require it (optional)
# Some ATS require SMS verification the agent can't complete on its
# own; in that case it will pause or mark the row Error regardless.
phone:

## Notes / limits for the agent (optional)
# e.g. "Only create accounts on Greenhouse and Lever, nowhere else."
# e.g. "Never accept marketing/newsletter opt-ins."
notes:
- Never opt into marketing or newsletter checkboxes.
- If a site demands a credential not listed above, stop and mark Error.
